import React from 'react'

let Right = ({ children }) => (
	<div className='clearfix'>
        <div className='pull-right'>
            {children}
        </div>
    </div>
);

export default Right;
